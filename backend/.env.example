# Local development database; create the app schema before running migrations.
DATABASE_URL=postgresql://postgres:local-dev-only@localhost:5433/image_generator
# Generate with: openssl rand -hex 32
JWT_SECRET=
ACCESS_EXPIRES=1h
OPENAI_API_KEY=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=eu-west-2
AWS_BUCKET_NAME=
PORT=3000
